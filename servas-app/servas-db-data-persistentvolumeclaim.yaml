apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: servas-db-data
  name: servas-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
